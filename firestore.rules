rules_version = '2';

// All reads and writes go through the server (Firebase Admin SDK).
// Browsers never touch Firestore directly, so nothing is allowed here.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
